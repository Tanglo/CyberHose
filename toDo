Now:
	- finish CHScheduleLine nextStartDateWith: and stopTimeWith:
		- requires CHSchedule dateWithCalendarDayFrom:AndTimeFrom:

Future:
 ? CHGPIOController - track which pins are currently being used
 ? make the non-initialiser class methods of CHSchedule private.
